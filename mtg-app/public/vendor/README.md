# Place OpenCV.js here for production (self-hosted, no third-party CDN).
#
# Download once:
#   curl -L -o public/vendor/opencv.js https://docs.opencv.org/4.8.0/opencv.js
#
# Without this file, the scanner falls back to the official CDN (dev only recommended).
