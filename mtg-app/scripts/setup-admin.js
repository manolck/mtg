#!/usr/bin/env node

/**
 * Ce script ne fait plus partie du projet.
 * L'authentification et les rôles admin sont gérés par PocketBase.
 *
 * Voir README.md — section « Premier administrateur ».
 */

console.log("L'application utilise PocketBase pour l'authentification.");
console.log('Créer un user avec roles: ["user","admin"] dans PocketBase Admin.');
console.log('Voir README.md (Premier administrateur).\n');
process.exit(0);
