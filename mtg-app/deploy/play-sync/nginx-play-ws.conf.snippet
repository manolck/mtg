# Snippet for the SPA host (e.g. mtg-app.duckdns.org).
# Include inside the HTTPS server { } block for the frontend.

location /play-ws {
    proxy_pass http://127.0.0.1:8091/play-ws;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
}

location /play-ws/health {
    proxy_pass http://127.0.0.1:8091/health;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
}
